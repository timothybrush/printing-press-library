// Copyright 2026 JimPresting and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/client"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/config"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/media-and-entertainment/substack-creator/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("categories_list",
			mcplib.WithDescription("List all global categories. Returns array of Category. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/categories", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("categories_newsletters",
			mcplib.WithDescription("List newsletters in a category. Required: category_id. Optional: limit (default: 30). Returns array of Publication. (public)"),
			mcplib.WithString("category_id", mcplib.Required(), mcplib.Description("Category ID")),
			mcplib.WithString("limit", mcplib.Description("Max newsletters")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/category/{category_id}/newsletters", []mcpParamBinding{{PublicName: "category_id", WireName: "category_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{"category_id"}),
	)
	s.AddTool(
		mcplib.NewTool("comments_add",
			mcplib.WithDescription("Add a comment to a post. Required: post_id, body. Returns the new Comment."),
			mcplib.WithString("post_id", mcplib.Required(), mcplib.Description("Post ID")),
			mcplib.WithString("body", mcplib.Required(), mcplib.Description("Comment body")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/comment", []mcpParamBinding{{PublicName: "post_id", WireName: "post_id", Location: "body"}, {PublicName: "body", WireName: "body", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("comments_list",
			mcplib.WithDescription("List comments on a post. Required: post_id. Optional: limit (default: 50). Returns array of Comment."),
			mcplib.WithString("post_id", mcplib.Required(), mcplib.Description("Post ID")),
			mcplib.WithString("limit", mcplib.Description("Max comments")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/post/{post_id}/comments", []mcpParamBinding{{PublicName: "post_id", WireName: "post_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{"post_id"}),
	)
	s.AddTool(
		mcplib.NewTool("comments_react",
			mcplib.WithDescription("React to a comment. Required: comment_id. Optional: emoji (default: ❤). Returns the new Reaction."),
			mcplib.WithString("comment_id", mcplib.Required(), mcplib.Description("Comment ID")),
			mcplib.WithString("emoji", mcplib.Description("Reaction emoji")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reaction/comment", []mcpParamBinding{{PublicName: "comment_id", WireName: "comment_id", Location: "body"}, {PublicName: "emoji", WireName: "emoji", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("dashboard_stats",
			mcplib.WithDescription("Aggregate dashboard stats for a publication you own. Required: publication_id. Optional: window (default: 30d). Returns the DashboardStats."),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID")),
			mcplib.WithString("window", mcplib.Description("Window (e.g. 7d, 30d, 90d)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/{publication_id}/stats", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "path"}, {PublicName: "window", WireName: "window", Location: "query"}}, []string{"publication_id"}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_create",
			mcplib.WithDescription("Create a new draft. Required: title. Optional: subtitle, body, section_id (plus 1 more). Returns the new Draft."),
			mcplib.WithString("title", mcplib.Required(), mcplib.Description("Draft title")),
			mcplib.WithString("subtitle", mcplib.Description("Draft subtitle")),
			mcplib.WithString("body", mcplib.Description("Body content (markdown or HTML)")),
			mcplib.WithString("section_id", mcplib.Description("Section ID to place the draft under")),
			mcplib.WithString("audience", mcplib.Description("Audience: everyone, only_paid, founding")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/drafts", []mcpParamBinding{{PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "subtitle", WireName: "subtitle", Location: "body"}, {PublicName: "body", WireName: "body", Location: "body"}, {PublicName: "section_id", WireName: "section_id", Location: "body"}, {PublicName: "audience", WireName: "audience", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_delete",
			mcplib.WithDescription("Delete a draft. Required: id. Destructive."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Draft ID")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/drafts/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_get",
			mcplib.WithDescription("Get a draft by ID. Required: id. Returns the Draft."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Draft ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/drafts/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_list",
			mcplib.WithDescription("List your drafts. Optional: limit (default: 50). Returns array of Draft."),
			mcplib.WithString("limit", mcplib.Description("Max drafts")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/post_management/drafts", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_preview",
			mcplib.WithDescription("Get an author-only preview link for a draft. Required: id. Returns the PreviewLink."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Draft ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/drafts/{id}/preview", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_publish",
			mcplib.WithDescription("Publish a draft. Required: id. Optional: send_email (default: true). Returns the new Post."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Draft ID")),
			mcplib.WithString("send_email", mcplib.Description("Send email to subscribers")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/drafts/{id}/publish", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "send_email", WireName: "send_email", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("drafts_update",
			mcplib.WithDescription("Update an existing draft. Required: id. Optional: title, subtitle, body. Returns the updated Draft."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Draft ID")),
			mcplib.WithString("title", mcplib.Description("New title")),
			mcplib.WithString("subtitle", mcplib.Description("New subtitle")),
			mcplib.WithString("body", mcplib.Description("New body")),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("PUT", "/drafts/{id}", []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "path"}, {PublicName: "title", WireName: "title", Location: "body"}, {PublicName: "subtitle", WireName: "subtitle", Location: "body"}, {PublicName: "body", WireName: "body", Location: "body"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("feed_list",
			mcplib.WithDescription("Get your feed. Optional: tab (default: for-you), limit (default: 30). Returns array of FeedItem."),
			mcplib.WithString("tab", mcplib.Description("Feed tab")),
			mcplib.WithString("limit", mcplib.Description("Max items")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/feed", []mcpParamBinding{{PublicName: "tab", WireName: "tab", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("images_upload",
			mcplib.WithDescription("Upload an image. Required: file. Returns the new UploadedImage."),
			mcplib.WithString("file", mcplib.Required(), mcplib.Description("Path to image file")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/image", []mcpParamBinding{{PublicName: "file", WireName: "file", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("me_follows",
			mcplib.WithDescription("Profiles you follow. Returns array of Profile."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/me/follows", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("me_recommendations",
			mcplib.WithDescription("Personal recommendations. Returns array of Publication."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/me/recommendations", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("me_subscriptions",
			mcplib.WithDescription("What you subscribe to. Returns array of Subscription."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/me/subscriptions", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notes_list",
			mcplib.WithDescription("List your recent notes. Optional: limit (default: 20), cursor. Returns array of Note."),
			mcplib.WithString("limit", mcplib.Description("Max notes")),
			mcplib.WithString("cursor", mcplib.Description("Pagination cursor")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/notes", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "cursor", WireName: "cursor", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notes_publish",
			mcplib.WithDescription("Publish a new note. Required: body. Returns the new Note."),
			mcplib.WithString("body", mcplib.Required(), mcplib.Description("Note body (markdown)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/comment/feed", []mcpParamBinding{{PublicName: "body", WireName: "body", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notes_react",
			mcplib.WithDescription("React to a note. Required: note_id. Optional: emoji (default: ❤). Returns the new Reaction."),
			mcplib.WithString("note_id", mcplib.Required(), mcplib.Description("Note ID")),
			mcplib.WithString("emoji", mcplib.Description("Reaction emoji")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reaction/comment", []mcpParamBinding{{PublicName: "note_id", WireName: "note_id", Location: "body"}, {PublicName: "emoji", WireName: "emoji", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("notes_reply",
			mcplib.WithDescription("Reply to a note. Required: note_id, body. Returns the new Note."),
			mcplib.WithString("note_id", mcplib.Required(), mcplib.Description("Parent note ID")),
			mcplib.WithString("body", mcplib.Required(), mcplib.Description("Reply body")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/comment/{note_id}/reply", []mcpParamBinding{{PublicName: "note_id", WireName: "note_id", Location: "path"}, {PublicName: "body", WireName: "body", Location: "body"}}, []string{"note_id"}),
	)
	s.AddTool(
		mcplib.NewTool("notes_restack",
			mcplib.WithDescription("Restack a note. Required: note_id. Returns the new Restack."),
			mcplib.WithString("note_id", mcplib.Required(), mcplib.Description("Note ID to restack")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/comment/{note_id}/restack", []mcpParamBinding{{PublicName: "note_id", WireName: "note_id", Location: "path"}}, []string{"note_id"}),
	)
	s.AddTool(
		mcplib.NewTool("posts_get",
			mcplib.WithDescription("Get a single post by slug. Required: slug. Returns the Post."),
			mcplib.WithString("slug", mcplib.Required(), mcplib.Description("Post slug")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/posts/{slug}", []mcpParamBinding{{PublicName: "slug", WireName: "slug", Location: "path"}}, []string{"slug"}),
	)
	s.AddTool(
		mcplib.NewTool("posts_list",
			mcplib.WithDescription("List your own posts (drafts + published). Optional: limit (default: 50), offset (default: 0). Returns array of Post."),
			mcplib.WithString("limit", mcplib.Description("Maximum results to return")),
			mcplib.WithString("offset", mcplib.Description("Pagination offset")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/posts", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("posts_react",
			mcplib.WithDescription("React to a post (heart it). Required: post_id. Optional: emoji (default: ❤). Returns the new Reaction."),
			mcplib.WithString("post_id", mcplib.Required(), mcplib.Description("Post ID to react to")),
			mcplib.WithString("emoji", mcplib.Description("Reaction emoji")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/reaction", []mcpParamBinding{{PublicName: "post_id", WireName: "post_id", Location: "body"}, {PublicName: "emoji", WireName: "emoji", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("posts_restack",
			mcplib.WithDescription("Restack a post to your Notes. Required: post_id. Optional: comment. Returns the new Restack."),
			mcplib.WithString("post_id", mcplib.Required(), mcplib.Description("Post ID to restack")),
			mcplib.WithString("comment", mcplib.Description("Optional comment with the restack")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/restack", []mcpParamBinding{{PublicName: "post_id", WireName: "post_id", Location: "body"}, {PublicName: "comment", WireName: "comment", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("posts_stats",
			mcplib.WithDescription("Engagement stats (likes/comments/restacks) for a post. Required: post_id. Returns the PostStats."),
			mcplib.WithString("post_id", mcplib.Required(), mcplib.Description("Post ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/post/{post_id}/stats", []mcpParamBinding{{PublicName: "post_id", WireName: "post_id", Location: "path"}}, []string{"post_id"}),
	)
	s.AddTool(
		mcplib.NewTool("profiles_get",
			mcplib.WithDescription("Get another user's profile by handle. Required: handle. Returns the Profile. (public)"),
			mcplib.WithString("handle", mcplib.Required(), mcplib.Description("User handle")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/profile/{handle}", []mcpParamBinding{{PublicName: "handle", WireName: "handle", Location: "path"}}, []string{"handle"}),
	)
	s.AddTool(
		mcplib.NewTool("profiles_me",
			mcplib.WithDescription("Get your own profile and publications. Returns the Profile."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/profile", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("publications_recommendations",
			mcplib.WithDescription("List publications recommended by a given publication. Required: publication_id. Returns array of Publication. (public)"),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/{publication_id}/recommendations", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "path"}}, []string{"publication_id"}),
	)
	s.AddTool(
		mcplib.NewTool("publications_search",
			mcplib.WithDescription("Search publications by query. Required: query. Optional: limit (default: 30), page (default: 1). Returns array of Publication. (public)"),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query")),
			mcplib.WithString("limit", mcplib.Description("Max results")),
			mcplib.WithString("page", mcplib.Description("Page number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/search", []mcpParamBinding{{PublicName: "query", WireName: "query", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sections_list",
			mcplib.WithDescription("List sections for a publication. Required: publication_id. Returns array of Section."),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/{publication_id}/sections", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "path"}}, []string{"publication_id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_add",
			mcplib.WithDescription("Add a subscriber by email. Required: email, publication_id. Returns the new Subscriber."),
			mcplib.WithString("email", mcplib.Required(), mcplib.Description("Subscriber email")),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID to add to")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/subscriber/add", []mcpParamBinding{{PublicName: "email", WireName: "email", Location: "body"}, {PublicName: "publication_id", WireName: "publication_id", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_count",
			mcplib.WithDescription("Get total subscriber counts (free + paid). Required: publication_id. Returns the SubscriberCount."),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/{publication_id}/subscriber_count", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "path"}}, []string{"publication_id"}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_export_free",
			mcplib.WithDescription("Export free subscribers as CSV. Optional: publication_id. Returns the ExportResult."),
			mcplib.WithString("publication_id", mcplib.Description("Publication ID (defaults to your primary)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/free_subscribers/export", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_export_paid",
			mcplib.WithDescription("Export paid subscribers as CSV. Optional: publication_id. Returns the ExportResult."),
			mcplib.WithString("publication_id", mcplib.Description("Publication ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/paid_subscribers/export", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("subscribers_list",
			mcplib.WithDescription("List subscribers for a publication you own. Required: publication_id. Optional: tier (default: all), limit (default: 100). Returns array of Subscriber."),
			mcplib.WithString("publication_id", mcplib.Required(), mcplib.Description("Publication ID")),
			mcplib.WithString("tier", mcplib.Description("Tier filter")),
			mcplib.WithString("limit", mcplib.Description("Max rows")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/publication/{publication_id}/subscribers", []mcpParamBinding{{PublicName: "publication_id", WireName: "publication_id", Location: "path"}, {PublicName: "tier", WireName: "tier", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{"publication_id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Run 'substack-creator-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Run 'substack-creator-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Run 'substack-creator-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "substack-creator-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "substack-creator-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "substack",
		"description": "Every Substack feature, plus a local SQLite database, full-text search across years of writing, and the only...",
		"archetype":   "content",
		"tool_count":  38,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion substack-creator-pp-cli binary.",
		"auth": map[string]any{
			"type": "cookie",
		},
		"resources": []map[string]any{
			{
				"name":        "categories",
				"description": "Substack content categories.",
				"endpoints":   []string{"list", "newsletters"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "comments",
				"description": "Comments on posts.",
				"endpoints":   []string{"add", "list", "react"},
				"searchable":  true,
			},
			{
				"name":        "dashboard",
				"description": "Publication analytics and engagement stats.",
				"endpoints":   []string{"stats"},
				"searchable":  true,
			},
			{
				"name":        "drafts",
				"description": "Manage post drafts.",
				"endpoints":   []string{"create", "delete", "get", "list", "preview", "publish", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "feed",
				"description": "Your reader feed.",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "images",
				"description": "Upload images to Substack's CDN.",
				"endpoints":   []string{"upload"},
				"searchable":  true,
			},
			{
				"name":        "me",
				"description": "Your own subscriptions, follows, and personal recommendations.",
				"endpoints":   []string{"follows", "recommendations", "subscriptions"},
				"syncable":    true,
			},
			{
				"name":        "notes",
				"description": "Substack Notes (microblog).",
				"endpoints":   []string{"list", "publish", "react", "reply", "restack"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "posts",
				"description": "Read and interact with your published posts.",
				"endpoints":   []string{"get", "list", "react", "restack", "stats"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "profiles",
				"description": "Read your own or another user's Substack profile.",
				"endpoints":   []string{"get", "me"},
				"searchable":  true,
			},
			{
				"name":        "publications",
				"description": "Search and inspect Substack publications globally.",
				"endpoints":   []string{"recommendations", "search"},
				"searchable":  true,
			},
			{
				"name":        "sections",
				"description": "Publication sections / categories.",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "subscribers",
				"description": "Manage your publication's subscribers.",
				"endpoints":   []string{"add", "count", "export_free", "export_paid", "list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Portfolio Dashboard", "command": "portfolio", "description": "One-screen status of every publication you own: subscriber count, paid count, last-published-at, drafts pending,...", "rationale": "Substack's web UI has no portfolio concept. Aggregating across publications requires a local SQLite store of synced...", "via": "mcp-command-mirror"},
			{"name": "Cross-Publication Twin", "command": "posts twin", "description": "Duplicate a published post into another publication you own as a draft. Preserves paywall markers, sections, and...", "rationale": "Substack's duplicate-post feature is single-publication. Cross-publication content reuse requires sniffing the...", "via": "mcp-command-mirror"},
			{"name": "Subscriber Churn Diff", "command": "subscribers churn", "description": "Diff two SQLite snapshots of your subscriber list: who newly subscribed, who unsubscribed, who upgraded free→paid,...", "rationale": "Substack reports subscriber count but never the named delta. The diff requires keeping two snapshots locally and...", "via": "mcp-command-mirror"},
			{"name": "Cross-Sell Candidates", "command": "subscribers cross-sell", "description": "SQL join across your publications' subscriber lists: emails paid on one publication but free or absent on the...", "rationale": "No single API call surfaces this. It requires joining /api/v1/subscriber/list across N publications keyed by email...", "via": "mcp-command-mirror"},
			{"name": "Best Posts Cross-Publication", "command": "posts best", "description": "Rank posts by views, likes, comments, or restacks within a window. Optionally aggregate across every publication you...", "rationale": "Substack ranks within one publication; this CLI ranks across all of yours from cached dashboard stats joined on...", "via": "mcp-command-mirror"},
			{"name": "Bilingual Pair Tracker", "command": "posts pairs", "description": "Record EN↔DE post pairings in a local table. `posts pair <en> <de>` adds; `posts pairs --missing` lists posts in...", "rationale": "No upstream concept of paired translations. A local post_pairs table joins to posts on slug; `--missing` is a...", "via": "mcp-command-mirror"},
			{"name": "Full-Text Grep", "command": "grep", "description": "FTS5 over post bodies + Notes + comments, ranked by bm25, returning snippets and source URLs. Optional scope...", "rationale": "Substack's site search is shallow and covers only posts. The local cache stores full bodies and an FTS5 index...", "via": "mcp-command-mirror"},
			{"name": "Schedule Board", "command": "schedule board", "description": "ASCII calendar of the next 30 days showing scheduled posts across every publication you own. Multi-pub editorial...", "rationale": "Substack's schedule view is per-publication and web-only. Reading scheduled_at across cached posts/drafts is a local...", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Portfolio Dashboard", "insight": "Substack's web UI has no portfolio concept. Aggregating across publications requires a local SQLite store of synced state that no other Substack tool maintains."},
			{"topic": "Cross-Publication Twin", "insight": "Substack's duplicate-post feature is single-publication. Cross-publication content reuse requires sniffing the source HTML, re-uploading images via /api/v1/image against a different subdomain, and POSTing /api/v1/drafts to the target."},
			{"topic": "Subscriber Churn Diff", "insight": "Substack reports subscriber count but never the named delta. The diff requires keeping two snapshots locally and joining on email — a SQLite move, not an API call."},
			{"topic": "Cross-Sell Candidates", "insight": "No single API call surfaces this. It requires joining /api/v1/subscriber/list across N publications keyed by email — a cross-source local join."},
			{"topic": "Best Posts Cross-Publication", "insight": "Substack ranks within one publication; this CLI ranks across all of yours from cached dashboard stats joined on engagement metrics."},
			{"topic": "Bilingual Pair Tracker", "insight": "No upstream concept of paired translations. A local post_pairs table joins to posts on slug; `--missing` is a one-line outer-join query."},
			{"topic": "Full-Text Grep", "insight": "Substack's site search is shallow and covers only posts. The local cache stores full bodies and an FTS5 index queries them all in milliseconds."},
			{"topic": "Schedule Board", "insight": "Substack's schedule view is per-publication and web-only. Reading scheduled_at across cached posts/drafts is a local query."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
